uniform float time; 
uniform vec3 color; 
// uniform float intensity; 


varying vec2 our_uv; 
void main() {
    //  vec2 center = vec2(0.5, 0.5); 
    //  float dist = distance(our_uv, center); 
    // float glow = pow(clamp(1.0 - dist, 0.0, 1.0), 3.0) * intensity; 
    //  gl_FragColor = vec4(glowColor, glow);
}
